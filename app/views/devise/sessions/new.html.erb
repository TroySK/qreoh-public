<div class="container container-top">
  <h1 class="text-lg">Logon To The QREOH World!</h1>
  <p>Welcome! This is your gateway to the world of QREOH. Like every secret society, to enter you must provide a password. Fortunately, you set your password ;). If you haven't already created one, signup and create your QREOH Pass.</p>
  <br>
  <div class="row">
    <div class="col-lg-offset-0 col-lg-4 col-md-offset-0 col-md-4 col-sm-offset-2 col-sm-8 col-xs-12">
      <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
      <div class="panel panel-primary">
        <div class="panel-heading">Login</div>
        <div class="panel-body">
          <%= f.error_notification %>
          <%= f.input :email, autofocus: true %>
          <%= f.input :password %>
          <%= f.input :remember_me, as: :boolean, :input_html => { :checked => true } if devise_mapping.rememberable? %>
          <%= link_to "Forgot your password?", new_password_path(resource_name) %>
        </div>
        <div class="panel-footer">
          <%= f.button :submit, "Login", :class => "btn btn-primary", :title => 'Down the rabbit hole!' %>
        </div>
      </div>
      <% end %>
    </div>
    <div class="col-lg-offset-0 col-lg-4 col-md-offset-0 col-md-4 col-sm-offset-2 col-sm-8 col-xs-12">
      <div class="panel panel-primary">
        <div class="panel-heading">Login or Sign up via Social</div>
        <div class="panel-body">
          <div class="row">
            <div class="col-xs-offset-1 col-xs-10">
              <br>
              <a href="<%= user_omniauth_authorize_path("facebook") %>" class="btn btn-facebook btn-block"><span><i class="fa fa-facebook fa-2x"></i></span><div>Sign In With Facebook</div></a>
              <br>
            </div>
          </div>
          <div class="row">
            <div class="col-xs-offset-1 col-xs-10">
              <a href="<%= user_omniauth_authorize_path("google_oauth2") %>" class="btn btn-google btn-block"><span><i class="fa fa-google fa-2x"></i></span><div>Sign In With Google+</div></a>
              <br>
            </div>
          </div>
        </div>
      </div>
    </div>
    <div class="col-lg-offset-0 col-lg-4 col-md-offset-0 col-md-4 col-sm-offset-2 col-sm-8 col-xs-12">
      <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
      <div class="panel panel-primary">
        <div class="panel-heading">Sign up</div>
        <div class="panel-body">
          <%= f.error_notification %>
          <%= f.input :email, autofocus: true %>
          <%= f.input :password, hint: ("8 characters minimum") %>
          <%= f.input :password_confirmation %>
        </div>
        <div class="panel-footer">
          <%= f.button :submit, "Sign up", :class => "btn btn-primary", :title => 'Become a QREOH Member' %>
        </div>
      </div>
      <% end %>
    </div>
  </div>
</div>
<%= render "devise/shared/links" %>
<% content_for :javascript_tracking do %>
if(typeof mixpanel !== 'undefined'){
  mixpanel.track("Viewed Login Page");
}
<% end %>